# Worker Pool Configuration
WORKER_POOL_SIZE=5

# Example .env for EnrollmentService with Worker Pool

# Application State
STATE=development
PORT=3002

# Database Configuration
DB_PASSWORD=123456
DB_NAME=dbtopicos
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres

# NATS Configuration
NATS_HOST=localhost
NATS_PORT=4222

# Kafka Configuration
KAFKA_HOST=localhost
KAFKA_PORT=9093
KAFKA_GROUP_ID=enrollment-workers

# Worker Pool Configuration
# Number of worker threads to spawn for parallel processing
# Recommended: Number of CPU cores or slightly higher
WORKER_POOL_SIZE=5
